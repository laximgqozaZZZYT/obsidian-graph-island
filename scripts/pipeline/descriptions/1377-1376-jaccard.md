## Description (subtask of 1376-hover-similar-suggest-top3)

src/utils/jaccard-similarity.ts を新規作成し、2つの集合 (Set<string>) を受け取り Jaccard 係数 |A∩B| / |A∪B| を返す純粋関数 `jaccardSimilarity(a: Set<string>, b: Set<string>): number` を export する。
  両方空集合の場合は 0 を返す。型は src/types.ts ではなく関数シグネチャ内で完結させる（Set<string>のみ）。
  tests/utils/jaccard-similarity.test.ts を新規作成し、以下を網羅:
  - 完全一致: 1 を返す
  - 完全不一致: 0 を返す
  - 部分一致: 既知の値（例 {a,b,c} ∩ {b,c,d} → 2/4 = 0.5）
  - 両方空: 0 を返す（NaN ガード）
  - 片方空: 0 を返す
  - 重複要素を含む配列ではなく Set として正しく扱われること
  既存ファイルの変更は禁止。God Object には触れない。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
