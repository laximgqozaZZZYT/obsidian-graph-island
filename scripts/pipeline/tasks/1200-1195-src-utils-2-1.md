---
priority: high
reported: 2026-04-24
status: pending
source: decomposed
parent: 1195-142-src-utils
depends: none
summary: カバレッジ計測して src/utils/ 低カバー2ファイルを特定し1ファイル目のテストを追加
---

## Description (subtask of 1195-142-src-utils)

1. `pnpm test:coverage` を実行し、`src/utils/` 配下で function coverage < 60% かつ pure な (Obsidian API 非依存) ファイルを 2 つ特定する。
     除外: 既にテスト済みの query-expr.ts の wildcard/fuzzy/edge-case, graph-filter.ts の境界値 (cycle105, cycle107で追加済み)。
     候補例: `src/utils/constants.ts`, `src/utils/array.ts`, `src/utils/string.ts`, `src/utils/color.ts`, `src/utils/math.ts`, `src/utils/path.ts` 等、存在するもの。
  2. 特定した 1 ファイル目に対して `tests/utils/<target1>.test.ts` を新規作成し、describe ブロック内で 8-12 件の it() を書く。
     - 境界値: empty input, single element, duplicates, self-reference
     - エラーパス: invalid input, null/undefined, missing fields
     - 既存テスト (`tests/utils/` 内) と重複しないこと
  3. コミットメッセージに「識別結果」(どのファイルが候補 2 つだったか) を含める —

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
