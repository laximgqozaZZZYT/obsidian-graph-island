---
priority: medium
reported: 2026-04-18
status: decomposed
source: decomposed
parent: 593-585-subtask
depends: none
summary: 親タスク594-585のdone遷移を検証し本タスクをクローズ
---

## Description (subtask of 593-585-subtask)

検証のみ (read-only中心、最後にステータス移動のみ書き込み)。

  1. `issues/done/594-585-graphviewcontainer-ts-god-object-8597.md` が存在し
     frontmatter の `status: done` であることを Read で確認。done に無い場合は
     本タスクを done に遷移させず、未完了として終了。
  2. `src/views/GraphViewContainer.ts` の現在行数を `wc -l` で取得し、
     CLAUDE.md の GOD OBJECT Policy 表の「Max Allowed: 8597」を超えていないか確認。
     超過していれば失敗として報告 (God Object肥大化は禁止)。
     減っていれば CLAUDE.md の Max Allowed を現行値に ratchet down 更新。
  3. `pnpm lint` と `pnpm test` を実行し、親タスクの成果が壊れていないことを確認。
  4. 上記すべて緑なら、本プレースホルダーissueの frontmatter を
     `status: decomposed` → `status: done` に更新し、
     ファイルを `issues/pending/` 等から `issues/done/` に移動してコミット。
  5. 新規ファイル追加・ロジック変更は一切行わない (GraphViewContainer.ts本体編集禁止)。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと

## Verification log (2026-04-18)

- parent not done
- 親タスク `594-585-graphviewcontainer-ts-god-object-8597.md` は `scripts/pipeline/tasks/` に存在 (`status: decomposed`)
- `scripts/pipeline/issues/done/594-585-*.md` は不在
- 本issueの status は `decomposed` のまま据え置き (未完了として残す)
- 親タスク完了後、再試行されるまで本タスクは done に遷移しない
