## Description (subtask of 1261-i18n-hardcoded-strings)

src/ 配下で `setText(`, `.textContent =`, `placeholder:` を grep し、
  t() でラップされていない文字列リテラルを全件特定する。
  各箇所について file:line と元の文字列を記録し、src/i18n.ts の既存命名規則
  (例: panel.*, toolbar.*, viewMode.*) に従ってキー名案を決める。
  調査結果を一時的に src/i18n-migration-notes.ts (新規) または
  既存の i18n.ts のコメントに TODO リストとして記録する。
  この時点ではまだコード変更しない(調査のみ)。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
