---
priority: high
reported: 2026-04-19
status: done
source: decomposed
parent: 134-dead-exports
depends: subtask-1
summary: カテゴリB (ファイル内のみ使用) のexportキーワード除去
---

## Description (subtask of 134-dead-exports)

カテゴリBに分類されたシンボルから `export` キーワードだけを除去 (関数/定数自体は残す)。
  例: `export function foo()` → `function foo()`。
  対象は src/utils/, src/layouts/, src/parsers/ 配下の純粋関数を優先。
  GOD OBJECT ファイル (GraphViewContainer, PanelBuilder, EdgeRenderer, RenderPipeline) は
  触らない。変更後 `pnpm test` と `pnpm lint` を通す。最低 4個の export を除去すること。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
