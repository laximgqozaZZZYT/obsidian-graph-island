---
priority: high
reported: 2026-04-18
status: pending
source: decomposed
parent: 707-675-subtask
depends: none
summary: 現在のカバレッジを測定し超過指標を特定
---

## Description (subtask of 707-675-subtask)

`pnpm test:coverage` を実行し、4指標 (Statements/Branches/Functions/Lines) の現在値を取得。
  vitest.config.ts の現行閾値と比較し、現在値が「閾値+1pt」以上の指標のみリストアップする。
  結果を `coverage/coverage-report.tmp.md` に記録（`coverage/` は `.gitignore:92` で除外済み）。
  例: S=28.67% 閾値=28 → 超過+0.67pt → 対象外 / B=27.19% 閾値=25.5 → 超過+1.69pt → 対象
  このファイルは次タスク (827) で参照し、完了時に削除する。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
