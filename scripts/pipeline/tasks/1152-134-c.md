---
priority: high
reported: 2026-04-19
status: in-progress
source: decomposed
parent: 134-dead-exports
depends: subtask-1
summary: カテゴリC (完全未使用) のシンボル削除
---

## Description (subtask of 134-dead-exports)

カテゴリCに分類されたシンボル (関数/定数/型) を丸ごと削除する。
  削除前に `grep -rn "シンボル名" src/ tests/ e2e/` で最終確認し、コメント内参照含めゼロであることを確認。
  GOD OBJECTファイルの行数を減らす方向は可だが、新規行追加は禁止。
  対象外: types.ts の型、i18n.ts のキー、public plugin API (manifest.json で参照されるもの)。
  最低 4個削除する。`pnpm test`, `pnpm build`, `pnpm lint` すべて PASS を維持。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
