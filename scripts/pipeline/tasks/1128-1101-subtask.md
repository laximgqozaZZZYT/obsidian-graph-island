---
priority: medium
reported: 2026-04-19
status: pending
source: decomposed
parent: 1101-1080-status-done
depends: none
summary: subtask
---

## Description (subtask of 1101-1080-status-done)

この issue は既に手順が明確で atomic（1セッション内で完結可能）なため、分割せず 1 SUBTASK としてまとめます。独立コミット可能な単位がこれ以上細かくならないため、これ以上の分解は churn を増やすだけです。

`★ Insight ─────────────────────────────────────`
- 分解のアンチパターン: 「ファイル探索」「Edit」「検証」を別 SUBTASK にすると、どれも単独でコミットできず、パイプラインの orchestration コストだけが増える
- 本 issue の本質は「status フィールドを1行書き換える」単一責務であり、検証ステップは同一コミット内に閉じるのが正しい粒度
- `replace_all=false` 指定は、frontmatter 以外に `status:` 文字列が本文に現れた場合の誤置換を防ぐ安全策
`─────────────────────────────────────────────────`

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
