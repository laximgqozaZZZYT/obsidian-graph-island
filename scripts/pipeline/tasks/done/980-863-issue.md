---
priority: high
reported: 2026-04-19
status: done
source: decomposed
parent: 863-740-issue-status-done
depends: none
summary: 対象issueファイルのパスとディレクトリ慣習を特定
---

## Description (subtask of 863-740-issue-status-done)

パイプライン実行時点の実リポジトリ状態を確認し、以下を特定する:
  1. 親issue `740-723-subtask` の実ファイルパス (`.claude/issues/pending/` or `in-progress/` 配下)
  2. 完了issueの移動先ディレクトリ名 (`done/` / `closed/` 等、既存の慣習)
  3. 対象ファイルの現在の frontmatter `status:` 値
  調査結果は標準出力に記載し、コード変更・コミットは行わない。
  `ls .claude/issues/` と `grep -r "status:" .claude/issues/` で確認。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
