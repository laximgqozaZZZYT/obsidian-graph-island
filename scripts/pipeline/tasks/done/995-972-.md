---
priority: medium
reported: 2026-04-19
status: cancelled
source: decomposed
parent: 972-956-639-626-subtask-issue-pending-done-git-m
depends: subtask-1
summary: 差分検証 + 単一コミット + 最終検証
---

## Description (subtask of 972-956-639-626-subtask-issue-pending-done-git-m)

1. Bash `git status --short` で差分が以下3つ (またはrename検出時2つ) のみであることを検証:
     - `D issues/pending/<filename>.md`
     - `A issues/done/<filename>.md` (または `R` で rename 検出)
     - 移動先ファイルの `status` 行1行のみの変更
     - 他ファイルの差分が1つでもあれば即中止
  2. Bash HEREDOC で `git add -A && git commit -m "chore: done <filename-without-ext>"` を実行
     - `--no-verify` / `--no-gpg-sign` は使用しない
  3. 最終検証:
     - `git status` が clean
     - `git log -1 --pretty=%s` がコミットメッセージと一致
     - `ls issues/done/<filename>.md` が存在し `issues/pending/<filename>.md` が存在しない
  
  制約:
  - push は行わない
  - location.reload() 禁止 (該当なしだが念のため)

`★ Insight ─────────────────────────────────────`
SUBTASK分割の判断基準: `git mv` と `git commit` を分けたのは、subtask-1で差分生成が失敗 (no-op exit) した場合にsubtask-2をスキップできるようにするため。パイプラインの各ステップが冪等に判定できる設計です。
`─────────────────────────────────────────────────`

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
