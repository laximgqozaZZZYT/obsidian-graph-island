---
priority: high
reported: 2026-04-18
status: done
source: decomposed
parent: 684-639-subtask-2-pass-fail
depends: none
summary: subtask-2 レポートの発見と読み取りスクリプト作成
---

## Description (subtask of 684-639-subtask-2-pass-fail)

subtask-2 (607-597 verify) の成果物を特定し読み取る bash スクリプトを新規作成する。
  - 探索対象 (優先順):
    1. `/tmp/607-597-subtask-2-verify.json` など subtask-2 が吐いた JSON レポート
    2. docs/memory/issues/ 配下 (または該当 issue queue ディレクトリ) の 607-597-*.md の frontmatter `status:` と本文の PASS/FAIL 行
  - 出力: stdout に以下 4 行を key=value 形式で出力して終了 (ファイル編集禁止)
    - status=done|blocked|unknown
    - pass=<件数>
    - fail=<件数>
    - executed=<YYYY-MM-DD>
  - レポートが見つからない場合は status=unknown / pass=0 / fail=0 / executed=(現在日付) として終了コード 0 で返す (次タスクの gate 側で判定する)。
  - grep/awk/sed と date コマンドのみ使用。新規依存追加禁止。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
