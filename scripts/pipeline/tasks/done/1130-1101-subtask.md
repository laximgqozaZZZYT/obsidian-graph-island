---
priority: medium
reported: 2026-04-19
status: cancelled
source: decomposed
parent: 1101-1080-status-done
depends: none
summary: subtask
---

## Description (subtask of 1101-1080-status-done)

で確認した実際の値)
  - new_string: `status: done`
  - replace_all: false
  - 他のフィールド (priority, reported, source, parent, depends, summary) には触らない

  検証:
  1. `git status --short` → 対象ファイル1件のみ `M` 表示
  2. `git diff <file>` → status 行のみ変更、`---` frontmatter 境界が保持
  3. Read tool で frontmatter 全体を再確認し、他フィールドが保持されていること

  受け入れ基準:
  - [ ] 対象ファイルの status が `done` になっている
  - [ ] 他のフィールドに変更がない
  - [ ] CLAUDE.md のルール (GOD OBJECT禁止, location.reload()禁止等) に違反しない

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
