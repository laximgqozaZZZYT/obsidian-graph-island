---
priority: medium
reported: 2026-04-18
status: done
source: decomposed
parent: 625-609-subtask
depends: subtask-1
summary: カバレッジラチェット閾値の検証と記録
---

## Description (subtask of 625-609-subtask)

`pnpm test:coverage` を実行し、プロジェクト全体のカバレッジが
  `vitest.config.ts` の閾値 (S28.6/B27.1/F25.4/L28.3) を
  下回っていないことを確認する。

  手順:
  1. `pnpm test:coverage 2>&1 | tee /tmp/coverage-gvc.log` を実行
  2. 出力末尾の `Coverage summary` から S/B/F/L 各値を取得
  3. `vitest.config.ts` の `coverage.thresholds` と照合
  4. 1 項目でも閾値未達なら親 issue に戻してブロック報告
     (カバレッジラチェットは CLAUDE.md の Forbidden Pattern)
  5. 全項目クリアなら issue に測定値を追記してコミット

  禁止事項:
  - カバレッジが未達でも `vitest.config.ts` の閾値を下げない
    (CLAUDE.md: 「Coverage ratchet: thresholds must never decrease」)
  - 単一ファイル(GraphViewContainer.ts)のカバレッジのみで判定しない
    (閾値はプロジェクト全体に適用される)

  Acceptance:
  - [ ] `pnpm test:coverage` がカバレッジ閾値クリアで完了
  - [ ] S/B/F/L 全値が `vitest.config.ts` 閾値以上
  - [ ] issue に測定値 (S_.__ B_.__ F_.__ L_.__) が記録されている

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
