# Technical Debt Report — 2026-06-14

## Executive Summary

The codebase is in reasonable health overall: ESLint is clean (0 errors, 3 minor warnings),
coverage is tracked by a ratchet mechanism, and most source modules have dedicated test files.
The two critical areas requiring attention are the **god object near-limit violations** and
**9 unpatched security advisories in devDependencies**.

---

## Priority-Ranked Findings

### P0 — Immediate Action Required

#### 1. God Objects at or Near Ratchet Limit

Two files are at or within 1 line of their enforced maximum:

| File | Current | Max Allowed | Headroom | Status |
|------|---------|------------|---------|--------|
| `src/views/EdgeRenderer.ts` | 2765 | 2765 | **0 lines** | 🔴 AT LIMIT |
| `src/views/RenderPipeline.ts` | 2656 | 2657 | **1 line** | 🔴 CRITICAL |
| `src/views/GraphViewContainer.ts` | 8502 | 8655 | 153 lines | 🟡 Caution |
| `src/views/PanelBuilder.ts` | 1715 | 2216 | 501 lines | 🟢 OK |

`EdgeRenderer.ts` and `RenderPipeline.ts` have essentially zero room for any feature
addition without triggering the forbidden pattern gate. Any PR that touches these files
with anything beyond a pure deletion will fail the CI ratchet check.

**Root cause in `EdgeRenderer.ts`**: A single method `invalidateBundles()` at line 1024
spans an estimated ~1741 lines — over 60% of the entire file. This is the #1 decomposition
target in the codebase.

**Root cause in `RenderPipeline.ts`**: `_runDensityCulling()` at line 2078 spans ~578 lines
and `getCanvasDimensions()` at line 283 spans ~198 lines.

### P1 — Security: 9 Vulnerabilities in DevDependencies

All vulnerabilities are confined to devDependencies (build toolchain), so the
**shipped plugin (`main.js`) is not affected at runtime**. However, developers running
`pnpm audit` in CI or locally are exposed, and the dev server attack surface is real
during local development.

| Severity | Count | Packages |
|----------|-------|---------|
| **High** | 4 | `vite` (2 CVEs), `picomatch` (ReDoS), `esbuild` (binary integrity) |
| **Moderate** | 5 | `vite` (path traversal), `esbuild` (CORS), `picomatch` (method injection), `postcss` (XSS), `brace-expansion` (DoS) |

**Recommended fixes:**

| Package | Vulnerable Range | Fix Version | Dependency Path |
|---------|-----------------|-------------|----------------|
| `vite` | `>=8.0.0 <=8.0.4` | `>=8.0.5` | `vitest > vite` |
| `postcss` | `<8.5.10` | `>=8.5.10` | `vitest > vite > postcss` |
| `picomatch` | `<4.0.4` | `>=4.0.4` | `typescript-eslint > picomatch` |
| `esbuild` | `<0.28.1` | `>=0.28.1` | `esbuild` (direct) |
| `brace-expansion` | `>=5.0.0 <5.0.6` | `>=5.0.6` | `eslint > minimatch > brace-expansion` |

All fixes are available via `pnpm update vitest esbuild --latest` + lockfile update.

### P2 — Massively Oversized Functions (Cognitive Complexity)

Functions exceeding 100 lines represent the highest cognitive load and refactoring risk.
Top offenders identified by static analysis:

**`src/views/EdgeRenderer.ts`**
| Line | Method | Est. Size |
|------|--------|-----------|
| 1024 | `invalidateBundles()` | ~1741 lines |

**`src/views/RenderPipeline.ts`**
| Line | Method | Est. Size |
|------|--------|-----------|
| 2078 | `_runDensityCulling()` | ~578 lines |
| 283 | `getCanvasDimensions()` | ~198 lines |
| 1397 | `createPixiNodes()` | ~181 lines |
| 1041 | `_cleanupCardTextAll()` | ~162 lines |
| 1675 | `_createTagLabel()` | ~122 lines |

**`src/views/PanelBuilder.ts`**
| Line | Method | Est. Size |
|------|--------|-----------|
| 939 | *(builder chain block)* | ~620 lines |
| 627 | *(builder chain block)* | ~250 lines |

**`src/views/GraphViewContainer.ts`**
| Line | Method | Est. Size |
|------|--------|-----------|
| 6948 | `_buildNodeColorFn()` | ~295 lines |
| 287 | `getLabelQualityScore()` | ~281 lines |
| 1979 | `_wireCanvasManagers()` | ~177 lines |
| 5658 | `autoFitView()` | ~151 lines |

### P3 — Untested Files

18 source files have no corresponding test file. Excluding barrel/type-only files,
the substantive gaps are:

**Runtime logic with no tests:**
- `src/utils/toast.ts` — toast notification helper (runtime UI side-effect)
- `src/utils/view-mode-map.ts` — view mode lookup table
- `src/utils/view-mode-sections.ts` — view mode UI section config
- `src/views/canvas2d/CanvasApp.ts` — canvas initialization logic
- `src/views/cluster-compare-helpers.ts` — cluster comparison helpers
- `src/views/panel-sections-filter-card-helpers.ts` — filter card UI helpers
- `src/views/panel-sections-layout-helpers.ts` — layout panel helpers
- `src/views/semantic-zoom-renderer.ts` — semantic zoom rendering

**WebGL stack (no tests at all):**
- `src/views/webgl/WebGLApp.ts`
- `src/views/webgl/WebGLContainer.ts`
- `src/views/webgl/buffer-pool.ts`

**Type/barrel files (low priority):**
- `src/types.ts`, `src/layouts/timeline-types.ts`, `src/i18n-migration-notes.ts`
- `src/views/canvas2d/index.ts`, `src/views/canvas2d/interfaces.ts`, `src/views/webgl/index.ts`

### P4 — ESLint Warnings (Low Priority)

ESLint passes with **0 errors** and only **3 warnings**, all in a single file:

```
src/i18n-migration-notes.ts:25  @typescript-eslint/no-unused-vars  'evaluateExpr'
src/i18n-migration-notes.ts:25  @typescript-eslint/no-unused-vars  'parseQueryExpr'
src/i18n-migration-notes.ts:76  @typescript-eslint/no-unused-vars  'addToMapSet'
```

These are intentional migration placeholders per the file's own comment. Prefix with `_`
or remove to clean up.

### P5 — TODO/FIXME/HACK Comments

Only **1** code comment of this type exists in the entire codebase:

```
src/i18n-migration-notes.ts:8  // through `t()` in src/i18n.ts. Each entry is a TODO for the follow-up
```

This is exceptionally clean — indicates good hygiene or completed follow-through on prior
tech debt items.

---

## God Object Growth Trend

| File | CLAUDE.md recorded | Current | Delta | Trend |
|------|-------------------|---------|-------|-------|
| `GraphViewContainer.ts` | 8652 | 8502 | **-150** | ↓ Improving |
| `PanelBuilder.ts` | 1719 | 1715 | **-4** | ↓ Improving |
| `RenderPipeline.ts` | 2657 | 2656 | **-1** | → Stable |
| `EdgeRenderer.ts` | 2765 | 2765 | **0** | → Stable |

`GraphViewContainer.ts` has seen the most improvement (-150 lines since the ratchet was set),
but is still the largest file in the codebase at 8502 lines. The ratchet mechanism is working
as designed — no file has grown beyond its baseline.

---

## Recommended Next Actions

### Immediate (this sprint)
1. **Bump `vitest`** to pick up `vite >=8.0.5` and `postcss >=8.5.10` — single lockfile
   change, zero code changes.
2. **Bump `esbuild`** to `>=0.28.1` — direct devDependency, single version bump.
3. **Fix ESLint warnings** in `src/i18n-migration-notes.ts` — prefix unused imports with `_`.

### Near-term (next 1-2 sprints)
4. **Extract `invalidateBundles()` from `EdgeRenderer.ts`** — existing task
   `200-godobj-extract-tech-debt` subtask 1292. This single extraction would open up
   significant room in the file. Target: extract the bundle invalidation logic into
   `src/views/BundleInvalidator.ts` or similar.
5. **Extract `_runDensityCulling()` from `RenderPipeline.ts`** — task 1293. Creates
   headroom before the next feature addition triggers the gate.

### Medium-term
6. **Add WebGL stack tests** — `WebGLApp.ts`, `WebGLContainer.ts`, `buffer-pool.ts` have
   zero test coverage. This is the largest untested rendering path.
7. **Add tests for `view-mode-map.ts` and `view-mode-sections.ts`** — view mode logic
   affects the entire panel layout and is exercised implicitly but not tested directly.
8. **Decompose `PanelBuilder.ts` builder chains** — the 620-line and 250-line builder
   blocks are prime candidates for extraction into per-section builder modules.

---

*Generated: 2026-06-14 by autonomous tech-debt analysis routine.*
*Tools used: static file analysis, `pnpm audit`, `pnpm lint`.*
