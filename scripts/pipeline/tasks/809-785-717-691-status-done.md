---
priority: high
reported: 2026-04-18
status: pending
source: decomposed
parent: 785-762-717-691-edit
depends: none
summary: 717-691 コミット履歴から status:done に変更されたファイルパスを特定
---

## Description (subtask of 785-762-717-691-edit)

親タスク 717-691-status-done-edit の実装痕跡を追跡し、Edit で status: done に
変更された tasks/ 配下のファイル絶対パスを 1 つ特定する。

手順:
1. `git log --all --oneline --grep="717-691"` および `--grep="691"` で候補コミット抽出
2. 見つかったコミットに対し `git show <hash> --stat` で変更ファイル一覧を確認し、
   tasks/*.md かつ status フィールド変更を含むものを選別
3. コミットが特定できない場合、フォールバックとして
   `Grep pattern="^status: done" path="tasks/" glob="*.md"` で候補絞り込み、
   `git log --all -- <候補ファイル>` で 717-691 関連の変更履歴を確認
4. 標準出力に `TARGET_FILE: /home/ubuntu/obsidian-plugins/obsidian-graph-island/tasks/<filename>.md` 形式で明示

制約: ファイル変更・コミット・ブランチ切替は一切行わない(git log/show/status と Grep/Read のみ)。
該当ファイルが複数ある場合は最も新しいコミットのものを採用し、全候補を併記。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
