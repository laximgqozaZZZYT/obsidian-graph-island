# Technical Debt Report — 2026-04-26

## Executive Summary

Two god-object ratchet limits are actively breached. Seven security vulnerabilities
exist in devDependencies. ESLint cannot run due to missing `node_modules`. Six source
files have no test coverage whatsoever. The single largest function spans 462 lines.

---

## Priority-Ranked Findings

### P0 — Ratchet Limits Breached (blocks autonomous gate)

| File | Current Lines | Ratchet Limit | Over By |
|------|--------------|---------------|---------|
| `src/views/RenderPipeline.ts` | 2657 | 2476 | **+181** |
| `src/views/EdgeRenderer.ts` | 2765 | 2702 | **+63** |
| `src/views/GraphViewContainer.ts` | 8652 | 8655 | −3 (barely safe) |
| `src/views/PanelBuilder.ts` | 1719 | 2216 | −497 (headroom) |

`RenderPipeline.ts` and `EdgeRenderer.ts` are above their CLAUDE.md ratchet caps.
Any further additions to these files are forbidden until the overages are trimmed.

### P1 — Security Vulnerabilities (devDependencies, 7 total)

All vulnerabilities are in the `vitest → vite → postcss` chain (devDependencies only;
not shipped in `main.js`). They still represent risk in CI environments and developer
machines.

**High severity (3)**

| Package | Advisory | Fix |
|---------|----------|-----|
| `vite` | `server.fs.deny` bypassed with special queries | Upgrade vite ≥ patched |
| `vite` | Arbitrary file read via dev server | Upgrade vite |
| `picomatch` | ReDoS via extglob patterns | Upgrade picomatch |

**Moderate severity (4)**

| Package | Advisory | Fix |
|---------|----------|-----|
| `esbuild` | Any website can issue arbitrary requests to dev server | Upgrade esbuild |
| `picomatch` | Method injection in POSIX character classes | Upgrade picomatch |
| `vite` | Path traversal in optimised deps `.map` handling | Upgrade vite ≥ 8.0.5 |
| `postcss` | XSS via unescaped `</style>` in CSS stringify output | Upgrade postcss ≥ 8.5.10 |

**Recommended action**: `pnpm update --latest vitest vite postcss esbuild`

### P2 — ESLint Inoperable

`pnpm lint` fails with `ERR_MODULE_NOT_FOUND: Cannot find package '@eslint/js'`
because `node_modules` is absent. Run `pnpm install` before any lint check.
Additionally, `package.json` lacks `"type": "module"`, causing a Node.js performance
warning every lint invocation.

### P3 — God Object: 10 Largest Functions

Identified by static line-count estimation (next-function boundary heuristic):

| Approx Lines | File | Function | Starts At |
|-------------|------|----------|-----------|
| 462 | `EdgeRenderer.ts` | `buildDirectionBundles` | L436 |
| 231 | `GraphViewContainer.ts` | `getLastRenderTime` | L299 |
| 183 | `RenderPipeline.ts` | `getDonutDisplayConfig` | L308 |
| 178 | `GraphViewContainer.ts` | `invalidateBundleCache` | L7215 |
| 177 | `GraphViewContainer.ts` | `_wireCanvasManagers` | L2013 |
| 113 | `PanelBuilder.ts` | `updateSyntaxPreview` | L709 |
| 108 | `GraphViewContainer.ts` | `_toggleHelpOverlay` | L1239 |
| 99 | `PanelBuilder.ts` | `_buildSettingsActionButtons` | L1304 |
| 97 | `RenderPipeline.ts` | `cullOverlappingLabels` | L1931 |
| 97 | `EdgeRenderer.ts` | `computeStrengthGlow` | L1642 |

`EdgeRenderer.ts:buildDirectionBundles` at ~462 lines is the single highest-priority
extraction target and alone would reduce `EdgeRenderer.ts` below its ratchet limit.

### P4 — Untested Source Files (confirmed, 6 files)

These files have no test file and are not imported by any existing test:

| File | Notes |
|------|-------|
| `src/views/semantic-zoom-renderer.ts` | Rendering logic — high risk if untested |
| `src/views/webgl/WebGLApp.ts` | WebGL bootstrap — complex init path |
| `src/views/webgl/WebGLContainer.ts` | WebGL container lifecycle |
| `src/views/panel-helpers.ts` | Panel utility helpers |
| `src/views/panel-sections-filter-card-helpers.ts` | Filter card UI helpers |
| `src/views/panel-sections-layout-helpers.ts` | Layout section helpers |

### P5 — Coverage Thresholds (low floor)

Current ratchet floors (from `vitest.config.ts`):

| Metric | Threshold |
|--------|-----------|
| Statements | 54.3% |
| Branches | 48.0% |
| Functions | 52.1% |
| Lines | 54.6% |

Branch coverage at 48% means roughly half of all conditional logic paths are
unverified. The thresholds were manually lowered on 2026-04-25 to recover from
drift — this represents a regression in the ratchet.

### P6 — TODO / FIXME / HACK Debt (low)

Only 1 comment found across all of `src/`:

```
src/i18n-migration-notes.ts:8  // through `t()` in src/i18n.ts. Each entry is a TODO for the follow-up
```

This is intentional documentation in a notes file — not an actionable debt item.

---

## God Object Growth Trend

| File | Previous Ratchet | Current Lines | Limit | Status |
|------|-----------------|---------------|-------|--------|
| `GraphViewContainer.ts` | 8424 | 8652 | 8655 | ⚠ 3 lines of headroom |
| `RenderPipeline.ts` | 2321 | 2657 | 2476 | ❌ Breached (+181) |
| `EdgeRenderer.ts` | — | 2765 | 2702 | ❌ Breached (+63) |
| `PanelBuilder.ts` | — | 1719 | 2216 | ✅ Under limit |

`GraphViewContainer.ts` went from 8424 to 8652 lines in the last ratchet cycle
(+228 lines, a 2.7% growth that consumed nearly all headroom). At this rate it
will breach its own limit within 1–2 feature additions.

---

## Recommended Next Actions

1. **[Immediate]** Extract `EdgeRenderer.ts:buildDirectionBundles` (L436, ~462 lines)
   into a dedicated module. This alone eliminates the `EdgeRenderer.ts` ratchet breach.

2. **[Immediate]** Extract `RenderPipeline.ts:getDonutDisplayConfig` (L308, ~183 lines)
   and `cullOverlappingLabels` (L1931, ~97 lines) into helper modules to bring
   `RenderPipeline.ts` below its 2476-line limit.

3. **[Short-term]** Run `pnpm update --latest vitest vite postcss esbuild` to resolve
   the 7 security advisories. These are devDependencies; no production behaviour should
   change, but verify `pnpm test` passes after the upgrade.

4. **[Short-term]** Run `pnpm install` and verify `pnpm lint` passes. Investigate
   adding `"type": "module"` to `package.json` to eliminate the Node.js module-type
   performance warning.

5. **[Medium-term]** Add tests for the 6 confirmed untested files, prioritising
   `semantic-zoom-renderer.ts` and `webgl/WebGLApp.ts` due to their complexity.

6. **[Medium-term]** Raise coverage branch threshold from 48.0% toward 55%+ over
   successive PRs to close the gap opened by the 2026-04-25 ratchet reset.

7. **[Ongoing]** `GraphViewContainer.ts` is 3 lines from its ratchet ceiling.
   No new logic should be added there until extractions reduce it meaningfully.
