---
priority: medium
reported: 2026-04-19
status: pending
source: decomposed
parent: 1101-1080-status-done
depends: subtask-2
summary: git diff検証と受け入れ基準チェック
---

## Description (subtask of 1101-1080-status-done)

1. `git status --short` 実行 → 対象ファイル1件のみ `M` で出ることを確認
  2. `git diff HEAD~1 -- <file>` 実行 → 変更行が status 行のみであることを確認
  3. `---` 区切りが破損していないこと、frontmatter構造が保持されていることを確認
  4. 他のファイルが誤って変更されていないことを確認
  5. 受け入れ基準チェックリスト全て満たしていればタスク完了
  6. 問題があれば該当箇所を修正して再コミット

★ Insight ─────────────────────────────────────
このタスクは「メタ作業」(他のissueのstatus更新)なので、テスト実行やビルドは不要です。検証はgit diffとfrontmatter構造確認で十分。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
