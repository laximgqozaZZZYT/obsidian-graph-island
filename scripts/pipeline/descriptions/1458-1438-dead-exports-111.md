## Description (subtask of 1438-dead-exports)

ts-prune または knip 相当の手段で、現在 export されているが
  プロジェクト内（src/, tests/）からimportされていない名前を全件列挙する。
  検出後、以下のカテゴリに分類して reports/dead-exports.md に記録:
    A. 安全に削除可: 関数/定数/クラスで参照ゼロ
    B. export解除のみ可: 同ファイル内では使用、外部参照ゼロ
    C. 保留: 型エクスポート/テスト経由参照/Obsidian API公開要件
  ファイルパス・行番号・カテゴリをテーブル形式で記録する。
  この時点ではコードは変更しない（調査フェーズ）。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
