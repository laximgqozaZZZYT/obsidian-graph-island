---
priority: medium
reported: 2026-04-19
status: pending
source: decomposed
parent: 1099-1079-issue-frontmatter-status-done
depends: none
summary: 親issue 1026-1014-639-626-subtask-status-done の frontmatter を status: done に更新
---

## Description (subtask of 1099-1079-issue-frontmatter-status-done)

1. `Glob` パターン `**/1026-1014-639-626-subtask-status-done*.md` で対象ファイルを特定 (`.claude/issues/pending/` または `issues/pending/` 配下を想定)
  2. ヒットが0件なら `**/*1026-1014-639-626*.md` でフォールバック検索し、該当ファイルのパスを確定
  3. `Read` で frontmatter 冒頭20行を確認し、現在の `status:` 値 (`pending` または `in-progress`) を把握
  4. `Edit` で `status: pending` (または `status: in-progress`) を `status: done` に置換。frontmatter 内でユニークになるよう、必要なら前後1行を含めた `old_string` にする (replace_all=false)
  5. ファイルが `pending/` 配下にあり、リポジトリ規約で `done/` ディレクトリへの移動が必要な場合のみ `git mv` で移動 (規約が不明確なら移動せず status 書き換えのみで完了)
  6. `git status` と `git diff` で変更が frontmatter 1行 (status のみ) に限定されていることを確認
  7. コミットメッセージ: `chore: done 1026-1014-639-626-subtask-status-done`

  受け入れ基準:
  - 対象ファイルの frontmatter `status:` が `done`
  - 他フィールド・本文・コード領域に変更なし
  - `pnpm test` / `pnpm build` の実行は不要 (ドキュメント変更のみ)

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
