---
priority: high
reported: 2026-04-19
status: pending
source: decomposed
parent: 135-onclose-timer-leak
depends: subtask-1
summary: onClose() クリーンアップのユニットテスト追加
---

## Description (subtask of 135-onclose-timer-leak)

`tests/views/` 配下に onClose() のクリーンアップ動作を検証するテストを追加 (新規ファイルまたは既存ファイルに追記)。

  テストケース:
  1. `onClose()` 呼び出し時に `clearTimeout` が `_saveTimer` の値で呼ばれること
  2. `onClose()` 呼び出し時に `cancelAnimationFrame` が `_zoomAnimId` の値で呼ばれること
  3. タイマー未設定時 (undefined) でも onClose() が例外を投げないこと

  vi.spyOn で `globalThis.clearTimeout` / `globalThis.cancelAnimationFrame` をモック化し、
  `tests/__mocks__/obsidian.ts` を使用。GraphViewContainer のインスタンスに
  `_saveTimer` と `_zoomAnimId` を直接セットしてから onClose() を呼ぶ。

  `pnpm test` で全既存テストがパスすることを確認する。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
