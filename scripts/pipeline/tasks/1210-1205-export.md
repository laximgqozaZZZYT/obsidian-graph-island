---
priority: high
reported: 2026-04-24
status: in-progress
source: decomposed
parent: 1205-145-cluster-force-ts-export
depends: none
summary: パーティション/バケット純粋関数を export し境界値テスト追加
---

## Description (subtask of 1205-145-cluster-force-ts-export)

src/layouts/cluster-force.ts で以下の内部関数に `export` を追加 (シグネチャ変更禁止):
  - `backlinkBucket(deg: number): string` (L2438)
  - `partitionNodes(nodes, groupBy, degrees)` (L2345)
  - `computeEffectiveColumnSpacing(...)` (L1538)

  新規 tests/cluster-force-pure.test.ts を作成し、4件以上の境界値テストを追加:
  - backlinkBucket: deg=0 / deg=1 / deg=境界値 / 大きな deg
  - partitionNodes: 空配列 / 単一ノード / groupBy が空文字 / 多様なグループ
  - computeEffectiveColumnSpacing: 単一ノード / 多ノード / 0 spacing

  既存の cluster-force*.test.ts とは命名を分けて重複を避ける。
  GraphNode 型は src/types.ts を import。

---

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
