---
priority: medium
reported: 2026-04-19
status: in-progress
source: decomposed
parent: 137-uncancellable-raf-chains
depends: subtask-2
summary: rAF キャンセルの単体テスト追加
---

## Description (subtask of 137-uncancellable-raf-chains)

新規テストファイル tests/views/GraphViewContainer.rafCancel.test.ts を作成。
  - panToNode 実行中に onClose() を呼ぶと rAF がキャンセルされること (cancelAnimationFrame モック呼び出し検証)
  - _animateToNode 実行中に onClose() を呼ぶと setHighlightedNodeId/applyHover が追加で呼ばれないこと
  - _fadeNodeAlpha を同一ノードに連続呼び出しすると、前回の rAF がキャンセルされること
  - onClose() で全ノードの `_fadeRafId` がキャンセル & null になること
  tests/__mocks__/obsidian.ts の既存モックを利用。`global.requestAnimationFrame` / `cancelAnimationFrame` は vi.fn() でスパイ。
  pnpm test で全パスすること。カバレッジしきい値を下げないこと。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
