# Technical Debt Report — 2026-05-17

> Generated from `main` @ `28966df` · obsidian-graph-island v0.6.0

---

## Executive Summary

| Category | Status | Severity |
|---|---|---|
| God object pressure | RenderPipeline.ts at 1 line below limit; EdgeRenderer.ts at exact limit | **CRITICAL** |
| Emerging god objects | cluster-force.ts (3570 lines) has no size limit set | HIGH |
| Security (dev deps) | 7 vulnerabilities (3 high, 4 moderate) — devDependencies only | HIGH |
| Untested god objects | All 4 god object files lack a corresponding test file by basename | HIGH |
| Unused imports | 3 dead imports in GraphViewContainer.ts flagged by ESLint | MEDIUM |
| Coverage floors | statements 54.3%, branches 48.0% — low absolute values | MEDIUM |
| Bundle headroom | 779.5 KiB / 800 KiB budget — 20.5 KiB remaining | LOW |
| TODO/FIXME/HACK | 1 comment found (migration note, intentional) | LOW |

---

## 1. God Object Sizes vs Ratchet Limits (CRITICAL)

| File | Current Lines | Ratchet Limit | Headroom | Status |
|---|---|---|---|---|
| `src/views/GraphViewContainer.ts` | 8,502 | 8,655 | 153 | OK |
| `src/views/RenderPipeline.ts` | 2,656 | 2,657 | **1** | ⚠️ CRITICAL |
| `src/views/EdgeRenderer.ts` | 2,765 | 2,765 | **0** | 🔴 AT LIMIT |
| `src/views/PanelBuilder.ts` | 1,715 | 2,216 | 501 | OK |

**RenderPipeline.ts and EdgeRenderer.ts have effectively zero room.** Any feature addition or Prettier reflow (as happened in Phase E2, which added +181 lines) will immediately break the autonomous gate and require another ratchet re-baseline.

### Recent Growth Trend (GraphViewContainer.ts)

| Commits | Line Count |
|---|---|
| 15+ recent commits | 8,502 (stable) |
| ~commit `3ab0892` and before | 8,472 |

GraphViewContainer grew by 30 lines across the Phase Q formatting commit and has been frozen since. The ratchet at 8,655 gives 153 lines of headroom — enough for a few PRs before the next crisis.

### Emerging God Objects (No Ratchet Set)

| File | Lines | Risk |
|---|---|---|
| `src/layouts/cluster-force.ts` | 3,570 | Above EdgeRenderer's ratchet limit; no cap |
| `src/i18n.ts` | 2,115 | Growing with every i18n addition |
| `src/views/panel-widgets.ts` | 1,811 | No cap; PanelBuilder's extracted peer |
| `src/types.ts` | 1,775 | Central type file; grows with every feature |

`cluster-force.ts` at 3,570 lines is already larger than any god object that has a ratchet. It should be added to the CLAUDE.md God Object table immediately.

---

## 2. Largest Functions in God Object Files

Functions with >70 lines (candidates for extraction):

### `src/views/GraphViewContainer.ts`

| Function | Lines | Location |
|---|---|---|
| `_wireCanvasManagers()` | 172 | line 1,979 |
| `_toggleHelpOverlay()` | 105 | line 1,208 |
| `drawSunburstArcs()` | 102 | line 5,041 |
| `_registerWorkspaceEvents()` | 94 | line 1,349 |
| `doRender()` | 92 | line 6,667 |
| `destroyPixi()` | 87 | line 1,716 |
| `_setupGraphicsLayers()` | 87 | line 1,889 |
| `_renderAnnotation()` | 87 | line 1,540 |
| `drawEnclosures()` | 85 | line 4,732 |
| `applyHover()` | 75 | line 3,878 |

**`_wireCanvasManagers()` at 172 lines is the largest single function** — wiring logic could be split into per-subsystem init helpers or moved to dedicated manager constructors.

### `src/views/RenderPipeline.ts`

| Function | Lines | Location |
|---|---|---|
| `cullOverlappingLabels()` | 95 | line 1,930 |
| `redrawNodeBatch()` | 73 | line 745 |
| `_buildBatchContext()` | 72 | line 852 |
| `drawNodeCircle()` | 57 | line 675 |

`cullOverlappingLabels()` is listed in CLAUDE.md as a decomposition target ("extract: LOD logic, culling logic"). This remains unactioned.

---

## 3. Untested Source Files

Files with no matching test file (by basename). 43 files total have no direct test coverage:

### God Objects (all 4 untested by basename)

- `src/views/GraphViewContainer.ts` — coverage only via `gvc-pure-functions.test.ts`, `gvc-helpers.test.ts`, etc.
- `src/views/RenderPipeline.ts` — split-test files exist but no `RenderPipeline.test.ts`
- `src/views/EdgeRenderer.ts` — no `EdgeRenderer.test.ts`
- `src/views/PanelBuilder.ts` — no `PanelBuilder.test.ts`

### High-Priority Untested Files (>500 lines, no test)

| File | Lines |
|---|---|
| `src/views/InteractionManager.ts` | 1,327 |
| `src/views/LabelManager.ts` | 819 |
| `src/views/LayoutController.ts` | 750 |
| `src/views/GuideRenderer.ts` | 765 |
| `src/views/EnclosureRenderer.ts` | 669 |
| `src/views/CableTrayRenderer.ts` | 1,435 |
| `src/views/SnapshotManager.ts` | ~300 |
| `src/views/SearchOrchestrator.ts` | ~200 |

### Other Untested Files

`src/views/canvas2d/{CanvasApp,CanvasContainer,CanvasGraphics,CanvasText,interfaces}.ts`, `src/views/webgl/{WebGLApp,WebGLContainer,WebGLGraphics,buffer-pool}.ts`, `src/views/cluster-compare-helpers.ts`, `src/views/semantic-zoom-renderer.ts`, `src/views/panel-sections-filter-card-helpers.ts`, `src/views/panel-sections-layout-helpers.ts`, `src/i18n-migration-notes.ts`, `src/layouts/RoadNetworkBuilder.ts`, `src/layouts/timeline-types.ts`, `src/types.ts`, `src/utils/toast.ts`, `src/utils/view-mode-map.ts`, `src/utils/view-mode-sections.ts`.

Note: many of these are covered indirectly by integration-style tests (e.g. `tests/canvas-container.test.ts` covers `CanvasContainer.ts`). Direct basename-matched test files are the gap tracked here.

---

## 4. Security Audit (`pnpm audit`)

**7 vulnerabilities total — all in devDependencies (no production exposure).**

### High Severity (3)

| Package | Vulnerability | Fix |
|---|---|---|
| `vite` (via `vitest`) | `server.fs.deny` bypass with query strings — GHSA-v2wj-q39q-566r | Upgrade `vite` ≥ 8.0.5 |
| `vite` (via `vitest`) | Arbitrary file read via dev server WebSocket — GHSA-p9ff-h696-f583 | Upgrade `vite` ≥ 8.0.5 |
| `picomatch` (via `typescript-eslint`) | ReDoS via extglob quantifiers — GHSA-c2c7-rcm5-vvqj | Upgrade `picomatch` ≥ 4.0.4 |

### Moderate Severity (4)

| Package | Vulnerability | Fix |
|---|---|---|
| `vite` (via `vitest`) | Path traversal in optimized deps `.map` handling — GHSA-4w7w-66w2-5vf9 | Upgrade `vite` ≥ 8.0.5 |
| `esbuild` | Dev server allows cross-origin requests — GHSA-67mh-4wv8-2f99 | Upgrade `esbuild` ≥ 0.25.0 |
| `postcss` (via `vite`) | XSS via unescaped `</style>` in stringify output — GHSA-qx2v-qp2m-jg93 | Upgrade `postcss` ≥ 8.5.10 |
| `picomatch` (via `typescript-eslint`) | Method injection in POSIX character classes — GHSA-3v7f-55p6-f55p | Upgrade `picomatch` ≥ 4.0.4 |

**Mitigation**: These vulnerabilities affect the dev server and build toolchain only; they do not ship in the Obsidian plugin `main.js`. No user data is at risk. However, upgrading `vite` (to ≥ 8.0.5) and `esbuild` (to ≥ 0.25.0) via `pnpm update` would clear 6 of the 7 findings. The `picomatch` issue resolves via `typescript-eslint` update.

---

## 5. ESLint Warnings

**3 warnings, 0 errors.** All in `src/views/GraphViewContainer.ts`.

| Line | Symbol | Rule |
|---|---|---|
| 25:29 | `evaluateExpr` | `@typescript-eslint/no-unused-vars` — imported but never used |
| 25:43 | `parseQueryExpr` | `@typescript-eslint/no-unused-vars` — imported but never used |
| 76:26 | `addToMapSet` | `@typescript-eslint/no-unused-vars` — imported but never used |

These are dead imports. Removing them shrinks `GraphViewContainer.ts` by 0–3 lines (import line consolidation) and reduces false-dependency surface. Each removal should be validated as the symbols may have been used in code that was extracted to helper files.

Note: `pnpm lint` emits a Node.js warning about missing `"type": "module"` in `package.json`. This is cosmetic and does not affect correctness.

---

## 6. Coverage Thresholds

Current floors from `vitest.config.ts`:

| Metric | Floor | Industry Typical |
|---|---|---|
| Statements | 54.3% | 70–80% |
| Branches | 48.0% | 60–75% |
| Functions | 52.1% | 70–80% |
| Lines | 54.6% | 70–80% |

All floors are ~15–25 pp below typical targets. The CLAUDE.md ratchet note (Phase E1) explains these were lowered 0.1–0.3% in April 2026 after main drifted below the previous ratchet. The autonomous pipeline's test-addition tasks (e.g. `1270-145-pnpm-test-coverage-src-5`) are the intended recovery path. No immediate action required, but the gap is wide enough that regressions can hide inside it.

---

## 7. Bundle Size

| Metric | Value |
|---|---|
| Current bundle | 779.5 KiB (798,203 bytes) |
| Budget cap | 800 KiB (819,200 bytes) |
| Headroom | **20.5 KiB** |

The bundle is 97.4% of budget. Two medium-sized feature additions could push it over. Consider auditing imports in `cluster-force.ts` (3,570 lines; likely the largest contributor after GraphViewContainer) and evaluating whether any layout algorithm can be lazy-loaded.

---

## 8. TODO / FIXME / HACK Comments

Only **1** comment found across `src/`:

```
src/i18n-migration-notes.ts:8  // through `t()` in src/i18n.ts. Each entry is a TODO for the follow-up
```

This is an intentional migration tracker, not a deferred bug. Codebase is otherwise clean of ad-hoc debt markers — good discipline.

---

## Recommended Next Actions (Priority Order)

### P0 — Immediate (before next feature PR)

1. **Extract from EdgeRenderer.ts and RenderPipeline.ts** (tasks `1292/1293/1295/1296` in the pipeline). Both files are at or within 1 line of their limits. Any Prettier reflow or single-line addition will break the gate. Target: move cable-tray rendering out of `EdgeRenderer.ts` and LOD/culling logic out of `RenderPipeline.ts`.

2. **Add `cluster-force.ts` to the CLAUDE.md God Object table** with a ratchet of 3,570. It is already larger than the god objects that have limits. Without a ratchet it will grow unbounded.

### P1 — This Sprint

3. **Upgrade `vite` ≥ 8.0.5, `esbuild` ≥ 0.25.0** via `pnpm update vite esbuild` — clears 6 of 7 security findings. Dev-only risk but good hygiene.

4. **Remove 3 dead imports from GraphViewContainer.ts** (`evaluateExpr`, `parseQueryExpr`, `addToMapSet` at lines 25–76). Each is flagged by ESLint. Verify by grep before deleting.

### P2 — Next Sprint

5. **Extract `_wireCanvasManagers()` (172 lines)** from GraphViewContainer.ts into a dedicated `CanvasWiring` module. This is the single largest function in the codebase by the analysis above and a clear candidate for the Phase E1 decomposition target "extract: snapshot, export, filter orchestration."

6. **Add direct tests for `InteractionManager.ts` (1,327 lines) and `CableTrayRenderer.ts` (1,435 lines)** — two large untested files where bugs would have wide impact.

### P3 — Ongoing

7. **Continue autonomous test-addition to recover coverage floors** back toward the pre-E1 ratchet values (statements ~54.6%, branches ~48.3%, etc.). The gap from typical 70–80% targets is structural and requires sustained effort.

8. **Monitor bundle size** after each feature PR. At 97.4% of budget, any bundled dependency addition needs explicit justification per CLAUDE.md conventions.
