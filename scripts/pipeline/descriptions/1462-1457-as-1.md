## Description (subtask of 1457-type-assertions)

`grep -rn "as " src/ --include="*.ts"` で 230 箇所の `as T` キャストを抽出し、ファイル別件数を `tmp/as-cast-distribution.txt` に出力する。
  上位 3 ファイルを特定する。
  `src/utils/type-guards.ts` を新規作成し、頻出する型ガード関数を追加する (例: `isHTMLElement(x): x is HTMLElement`, `isNonNull<T>(x: T | null): x is T`, `isString(x): x is string`, `isRecord(x): x is Record<string, unknown>`)。型ガードはユニットテストを `tests/utils/type-guards.test.ts` に追加。
  上位 1 ファイル (おそらく GraphViewContainer.ts) で `as` キャストを 50 箇所程度、型ガード関数か正しい型注釈に置換する。GOD OBJECT ポリシーにより GraphViewContainer.ts の行数は増やさない (差分は ±0 を目標)。
  `pnpm test` と `pnpm lint` がグリーンであること。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
