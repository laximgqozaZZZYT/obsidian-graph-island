## Description (subtask of 1352-broken-node-settings-cleanup)

以下を精読し、UIには存在するが描画/レイアウト/フィルタリングに反映されていない設定 (dead settings) を列挙する。
  
  対象:
  - `src/types.ts` の GraphViewSettings / RenderThresholds 各フィールド
  - `src/views/PanelBuilder.ts` のスライダー/トグル定義
  - `src/views/GraphViewContainer.ts` / `src/views/RenderPipeline.ts` / `src/views/EdgeRenderer.ts` / `src/layouts/` でのフィールド参照箇所
  
  特に確認するもの:
  1. `nodeSize` (スライダー) と `nodeSizeByDegree` の関係 — どちらが最終的に node radius を決めているか、`nodeSize` が `nodeSizeByDegree` で上書きされて無視される経路を特定
  2. UI に出ているが grep でロジック側参照が無い / 参照はあるが分岐に到達しないフィールド
  3. #1340 で既に削除されたフィールドの残骸 (PanelBuilder にだけ残っているケース)
  
  成果物として `temp/dead-settings-1352.md` のような調査メモは作らず、本タスクの最終出力として「削除対象フィールド名のリスト」と「各フィールドの参照ファイル/行番号」をコミットメッセージまたは PR description ベースで報告する形でよい (実装は

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
