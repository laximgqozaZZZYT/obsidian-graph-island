# Technical Debt Report — 2026-04-03

Generated by automated analysis of `obsidian-graph-island`.

---

## Executive Summary

| Category | Count / Status |
|---|---|
| God objects exceeding 3 000 lines | 4 files (21 600 lines combined) |
| Untested `src/` files | 44 files |
| Large functions (> 100 lines) | 12 functions |
| Security vulnerabilities | 3 (1 high, 2 moderate) |
| ESLint config | None — ESLint v10 config missing |

---

## Priority-Ranked Findings

### P1 — Critical

#### 1. God Object: `GraphViewContainer.ts` (9 934 lines)

`GraphViewContainer` is by far the largest file in the codebase. It mixes view
orchestration, rendering dispatch, data pipeline, interaction handling, layout
scheduling, panel state, and export logic all in a single class. This creates a
high merge-conflict surface and makes isolated testing nearly impossible.

**Top 10 largest functions inside `GraphViewContainer.ts`:**

| Line | Function | Size |
|---:|---|---:|
| 4 879 | `_updateGroupByLabels()` | 312 lines |
| 5 623 | `drawTimelineBars()` | 209 lines |
| 9 711 | `_renderMatrixViewMode()` | 200 lines |
| 7 641 | `doRender()` | 175 lines |
| 2 055 | `_wireCanvasManagers()` | 167 lines |
| 3 830 | `applyHover()` | 145 lines |
| 6 678 | `_buildPanelCallbacks()` | 134 lines |
| 9 099 | `applySearch()` | 130 lines |
| 4 580 | `_buildEdgeDrawConfig()` | 126 lines |
| 5 199 | `_drawZoomAggregates()` | 121 lines |

**Recommendation:** Extract cohesive clusters into dedicated controllers.
Natural seams:
- `TimelineRenderController` (drawTimelineBars, matrix view)
- `SearchController` (applySearch, text-fade)
- `HoverController` (applyHover, highlight propagation)
- `EdgeDrawConfigBuilder` (pure function, easy to test)

---

#### 2. Security: 1 High-Severity Vulnerability

| Severity | Package | Issue | Fix |
|---|---|---|---|
| **high** | `picomatch >=4.0.0 <4.0.4` | ReDoS via extglob quantifiers (GHSA-c2c7-rcm5-vvqj) | Upgrade to `picomatch >=4.0.4` |
| moderate | `picomatch >=4.0.0 <4.0.4` | POSIX char-class method injection (GHSA-3v7f-55p6-f55p) | Same upgrade |
| moderate | `esbuild <=0.24.2` | Dev server CORS bypass (GHSA-67mh-4wv8-2f99) | Upgrade `esbuild` to `>=0.25.0` |

All three vulnerabilities are in **dev/build dependencies** (vitest → picomatch,
esbuild), so production plugin users are not directly at risk. However the
picomatch high-severity ReDoS could affect CI pipelines processing malicious
file paths.

**Recommendation:** Run `pnpm update picomatch esbuild` and pin to safe
versions.

---

### P2 — High

#### 3. God Object Growth Trend

| File | Lines | Concern |
|---|---:|---|
| `src/views/GraphViewContainer.ts` | **9 934** | ~3× larger than the next biggest file |
| `src/views/PanelBuilder.ts` | **4 377** | Contains 3 `buildSection()` overloads (106–156 lines each) |
| `src/views/EdgeRenderer.ts` | **3 853** | `drawEdgeSegment` / `drawEdgeDecorations` near-duplicate at 125–126 lines |
| `src/views/RenderPipeline.ts` | **3 436** | `cullOverlappingLabels()` is 178 lines |

There is no documented per-file line-limit convention (no CLAUDE.md found).
Establishing a soft limit of **500 lines** and a hard limit of **1 000 lines**
per file would contain future growth.

---

#### 4. Large / High-Complexity Functions

Functions over 100 lines are difficult to reason about and almost never covered
by unit tests:

| File | Line | Function | Lines |
|---|---:|---|---:|
| GraphViewContainer.ts | 4 879 | `_updateGroupByLabels()` | 312 |
| GraphViewContainer.ts | 5 623 | `drawTimelineBars()` | 209 |
| GraphViewContainer.ts | 9 711 | `_renderMatrixViewMode()` | 200 |
| RenderPipeline.ts | 2 762 | `cullOverlappingLabels()` | 178 |
| GraphViewContainer.ts | 7 641 | `doRender()` | 175 |
| GraphViewContainer.ts | 2 055 | `_wireCanvasManagers()` | 167 |
| GraphViewContainer.ts | 3 830 | `applyHover()` | 145 |
| GraphViewContainer.ts | 6 678 | `_buildPanelCallbacks()` | 134 |
| GraphViewContainer.ts | 9 099 | `applySearch()` | 130 |
| GraphViewContainer.ts | 4 580 | `_buildEdgeDrawConfig()` | 126 |
| EdgeRenderer.ts | 3 289 | `drawEdgeSegment()` | 126 |
| PanelBuilder.ts | 1 257 | `buildSection()` | 156 |

---

### P3 — Medium

#### 5. Untested Source Files (44 files)

The following `src/` files have no corresponding `tests/*.test.ts` file. The
`src/views/` directory is entirely untested via unit tests, which is the highest
risk given the complexity of those files.

**src/views/ (all untested — 20 files):**
```
src/views/DiffOverlay.ts
src/views/EdgeRenderer.ts
src/views/EmbeddedGraphRenderer.ts
src/views/EnclosureRenderer.ts
src/views/GraphViewContainer.ts
src/views/GuideRenderer.ts
src/views/InteractionManager.ts
src/views/KeyboardHandler.ts
src/views/LabelManager.ts
src/views/LayoutController.ts
src/views/LayoutTransition.ts
src/views/LegendRenderer.ts
src/views/Minimap.ts
src/views/NodeComparisonView.ts
src/views/NodeDetailView.ts
src/views/PanelBuilder.ts
src/views/RenderPipeline.ts
src/views/StatsRenderer.ts
src/views/canvas2d/CanvasApp.ts
src/views/canvas2d/CanvasContainer.ts
src/views/canvas2d/CanvasGraphics.ts
src/views/canvas2d/CanvasText.ts
src/views/canvas2d/index.ts
src/views/canvas2d/interfaces.ts
src/views/panel-widgets.ts
src/views/webgl/WebGLApp.ts
src/views/webgl/WebGLContainer.ts
src/views/webgl/WebGLGraphics.ts
src/views/webgl/buffer-pool.ts
src/views/webgl/index.ts
src/views/webgl/shaders.ts
```

**Other untested files:**
```
src/analysis/graph-analysis.ts
src/layouts/RoadNetworkBuilder.ts
src/layouts/arc.ts
src/layouts/cable-tray.ts
src/layouts/concentric.ts
src/layouts/force.ts
src/layouts/timeline-types.ts
src/layouts/tree.ts
src/main.ts
src/settings.ts
src/types.ts
src/utils/toast.ts
src/utils/view-mode-map.ts
src/utils/view-mode-sections.ts
```

**Coverage observation:** All layout implementations (`arc.ts`, `concentric.ts`,
`force.ts`, `tree.ts`, `cable-tray.ts`) are untested at the file level, relying
instead on integration-style tests. This makes regression detection harder.

---

#### 6. ESLint Configuration Missing

The project has no `eslint.config.js` (or legacy `.eslintrc.*`). Running
`npx eslint src/` fails immediately:

```
ESLint couldn't find an eslint.config.(js|mjs|cjs) file.
```

Without linting enforcement there is no automated check for:
- Unused variables / imports
- `any` type usage
- Missing return types
- Consistent code style

**Recommendation:** Add a minimal `eslint.config.mjs` with
`@typescript-eslint/eslint-plugin` and integrate it into the `test` script.

---

### P4 — Low

#### 7. TODO / FIXME / HACK Comments

`grep -rn 'TODO\|FIXME\|HACK' src/` returned **0 matches**. While this is
positive, it may also indicate that deferred work is not being tracked inline.
Consider adopting a lightweight inline marker convention and wiring it to CI.

---

## Recommended Next Actions

| Priority | Action | Effort |
|---|---|---|
| P1 | Upgrade `picomatch` to `>=4.0.4` and `esbuild` to `>=0.25.0` | ~30 min |
| P1 | Begin extracting `GraphViewContainer` into focused controllers | ongoing |
| P2 | Add `eslint.config.mjs` with TypeScript rules | ~2 h |
| P2 | Establish per-file line-limit convention (e.g. 500 soft / 1 000 hard) in `CLAUDE.md` | ~30 min |
| P3 | Add unit tests for extracted pure functions (`_buildEdgeDrawConfig`, `_updateGroupByLabels`, etc.) | per-sprint |
| P3 | Add at least smoke tests for all `src/layouts/*.ts` implementations | ~1 day |
| P4 | Wire `pnpm audit` into CI to catch future vulnerabilities automatically | ~1 h |

---

*Report generated: 2026-04-03 — `auto/tech-debt-20260403`*
