---
priority: medium
reported: 2026-04-19
status: done
source: decomposed
parent: 1014-992-issues-pending-639-626-subtask-md-status
depends: none
summary: subtask
---

## Description (subtask of 1014-992-issues-pending-639-626-subtask-md-status)

リポジトリを調査したところ、issue が参照する `issues/pending/` ディレクトリは存在せず、実体は `scripts/pipeline/tasks/` 配下にあります。また `639-626.*subtask` パターンは 50 件以上マッチするため、「1 ファイル特定 → status 置換」は実質的に 1 つの原子操作です。以下 1 タスクに分解します。

`★ Insight ─────────────────────────────────────`
- `issues/pending/` は現プロジェクトでは `scripts/pipeline/tasks/`(active) と `scripts/pipeline/tasks/done/` に置き換わっており、issue テキストの旧パスは歴史的参照
- パイプラインはタスクを done 移動する際に frontmatter の status 書き換えとファイル移動の双方を扱うが、この親 issue は「置換のみ、mv はしない」と明示
- replace_all=false で一致が 1 件のみであることを確認するのは、他 frontmatter キー (例: `summary: status done ...`) への誤爆防止
`─────────────────────────────────────────────────`

---

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
