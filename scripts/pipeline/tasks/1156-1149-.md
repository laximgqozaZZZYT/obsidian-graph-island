---
priority: high
reported: 2026-04-19
status: pending
source: decomposed
parent: 1149-139-baseline-node-count-2000
depends: subtask-2
summary: 一時トレースを除去してブランチをクリーンアップ
---

## Description (subtask of 1149-139-baseline-node-count-2000)

subtask-1 で追加した `console.debug('[nodecount-trace] ...')` 行を全て削除し、
  GraphViewContainer.ts の行数を元に戻す (差分 ±0)。
  `pnpm build` / `pnpm lint` / `pnpm test` が全てグリーンであることを確認。
  tmp-debug-nodecount.md は調査成果物として残し、次issue (実際の修正タスク) の根拠として参照できる状態にする。
  コミットメッセージ: `chore: remove nodecount investigation traces (issue 139-1)`。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
