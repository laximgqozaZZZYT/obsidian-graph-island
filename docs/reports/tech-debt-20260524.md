# Technical Debt Report — 2026-05-24

> Generated from static analysis of `main` at commit `01793d9`.

---

## Summary

| Category | Count / Status |
|---|---|
| God objects at/near limit | 2 files (EdgeRenderer at 0%, RenderPipeline at <0.1% headroom) |
| Untested source files | 45 files |
| Security vulnerabilities | 8 (3 high, 5 moderate) — devDeps only |
| ESLint warnings | 3 (unused imports in GraphViewContainer) |
| Hardcoded i18n strings | 39 candidates (tracked in i18n-migration-notes.ts) |
| Coverage floor (branches) | 48.0% — lowest threshold |

---

## Priority 1 — God Object Ratchet: Two Files at the Wall

### Current Sizes vs Limits

| File | Lines | Limit | Headroom | Status |
|---|---|---|---|---|
| `GraphViewContainer.ts` | 8,502 | 8,655 | 153 (1.8%) | ⚠ Shrinking |
| `PanelBuilder.ts` | 1,715 | 2,216 | 501 (22.6%) | ✅ Comfortable |
| `RenderPipeline.ts` | 2,656 | 2,657 | **1 line** | 🔴 At wall |
| `EdgeRenderer.ts` | 2,765 | 2,765 | **0 lines** | 🔴 At wall |

`EdgeRenderer.ts` and `RenderPipeline.ts` have exhausted their budgets entirely. Any line-adding change to these files will break the autonomous gate and require another ratchet re-baseline (which the CLAUDE.md history shows has happened twice already). The recommended path is extraction *before* the next feature lands in these files.

### Top 10 Largest Methods in God Objects

| Lines | File | Method | Line |
|---|---|---|---|
| 330 | `EdgeRenderer.ts` | `_drawTrunkWires()` | 1479 |
| 289 | `EdgeRenderer.ts` | `_drawNonCabledEdge()` | 2477 |
| 202 | `EdgeRenderer.ts` | `pushSrcEntry()` | 915 |
| 197 | `GraphViewContainer.ts` | *(large method block)* | 3345 |
| 192 | `GraphViewContainer.ts` | `_wireCanvasManagers()` | 1979 |
| 183 | `GraphViewContainer.ts` | *(large method block)* | 3060 |
| 178 | `RenderPipeline.ts` | *(large method block)* | 1849 |
| 150 | `EdgeRenderer.ts` | `_drawNonCabledEdge()` | 2323 |
| 146 | `GraphViewContainer.ts` | *(large method block)* | 4452 |
| 143 | `GraphViewContainer.ts` | `_drawPathfinderOverlay()` | 4673 |

`EdgeRenderer._drawTrunkWires()` at 330 lines is the single largest method in the codebase — a strong extraction target. Splitting it into `_drawTrunkBranches()`, `_drawTrunkJunctions()`, and `_applyTrunkStyle()` would immediately reclaim headroom.

### Max Brace-Nesting Depth

| File | Max Depth | At Line |
|---|---|---|
| `GraphViewContainer.ts` | **8** | 7210 |
| `PanelBuilder.ts` | 7 | 1262 |
| `EdgeRenderer.ts` | 5 | 1453 |
| `RenderPipeline.ts` | 5 | 704 |

Depth-8 nesting in `GraphViewContainer.ts:7210` is a cognitive complexity hotspot; every additional conditional at that point is very hard to reason about.

---

## Priority 2 — Untested Source Files (45 files)

The following source files have no corresponding test file. High-impact targets (views, renderers) are listed first:

### Views — Class Files (no test coverage signal)
- `src/views/CableTrayRenderer.ts` (1,435 lines)
- `src/views/InteractionManager.ts` (1,327 lines)
- `src/views/LabelManager.ts` (819 lines)
- `src/views/GuideRenderer.ts` (765 lines)
- `src/views/LayoutController.ts` (750 lines)
- `src/views/EdgeRenderer.ts` (2,765 lines) ← god object, no dedicated test file
- `src/views/RenderPipeline.ts` (2,656 lines) ← god object, no dedicated test file
- `src/views/PanelBuilder.ts` (1,715 lines) ← god object, no dedicated test file
- `src/views/GraphViewContainer.ts` (8,502 lines) ← god object, no dedicated test file
- `src/views/EdgeLabelRenderer.ts`
- `src/views/EmbeddedGraphRenderer.ts`
- `src/views/EnclosureRenderer.ts`
- `src/views/ExportManager.ts`
- `src/views/DiffOverlay.ts`
- `src/views/KeyboardHandler.ts`
- `src/views/LayoutTransition.ts`
- `src/views/LegendRenderer.ts`
- `src/views/Minimap.ts`
- `src/views/NodeComparisonView.ts`
- `src/views/NodeDetailView.ts`
- `src/views/RenderHelpers.ts`
- `src/views/SearchOrchestrator.ts`
- `src/views/SnapshotManager.ts`
- `src/views/StatsRenderer.ts`
- `src/views/cluster-compare-helpers.ts`
- `src/views/panel-sections-filter-card-helpers.ts`
- `src/views/panel-sections-layout-helpers.ts`
- `src/views/semantic-zoom-renderer.ts`

### Canvas2D / WebGL Renderers (no tests)
- `src/views/canvas2d/CanvasApp.ts`
- `src/views/canvas2d/CanvasContainer.ts`
- `src/views/canvas2d/CanvasGraphics.ts`
- `src/views/canvas2d/CanvasText.ts`
- `src/views/canvas2d/index.ts`
- `src/views/canvas2d/interfaces.ts`
- `src/views/webgl/WebGLApp.ts`
- `src/views/webgl/WebGLContainer.ts`
- `src/views/webgl/WebGLGraphics.ts`
- `src/views/webgl/buffer-pool.ts`
- `src/views/webgl/index.ts`

### Layouts / Utils / Other
- `src/layouts/RoadNetworkBuilder.ts`
- `src/layouts/timeline-types.ts` *(type-only, low priority)*
- `src/types.ts` *(type-only, low priority)*
- `src/utils/toast.ts`
- `src/utils/view-mode-map.ts`
- `src/utils/view-mode-sections.ts`
- `src/i18n-migration-notes.ts` *(investigation artifact, do not import)*

**Note:** Several god-object files (`EdgeRenderer`, `RenderPipeline`, `PanelBuilder`, `GraphViewContainer`) are exercised indirectly through many existing test files (e.g., `tests/edge-renderer.test.ts`, `tests/render-pipeline.test.ts`, etc.). Coverage signals are present but not through dedicated per-class files. The 45 files here strictly lack a basename-matched test file — actual coverage may be higher than zero for some.

---

## Priority 3 — Coverage Floors

Current thresholds (from `vitest.config.ts`):

| Metric | Floor |
|---|---|
| Statements | 54.3% |
| Branches | **48.0%** |
| Functions | 52.1% |
| Lines | 54.6% |

Branch coverage at 48% is the weakest dimension — fewer than half of all code branches are exercised by the test suite. The ratchet re-baseline on 2026-04-25 lowered floors by 0.1–0.3% to match actuals after drift, meaning the trend was downward. Autonomous test additions are expected to restore previous floors; tracking the delta each sprint is recommended.

---

## Priority 4 — Security Vulnerabilities

`pnpm audit` reports **8 vulnerabilities** (all in devDependencies — no production risk):

| Severity | Package | Issue | Fix |
|---|---|---|---|
| 🔴 High | `picomatch` | ReDoS via extglob quantifiers (GHSA-c2c7-rcm5-vvqj) | Upgrade to ≥4.0.4 |
| 🔴 High | `vite` | `server.fs.deny` bypassed with queries (GHSA-v2wj-q39q-566r) | Upgrade to ≥8.0.5 |
| 🔴 High | `vite` | Arbitrary file read via dev server WebSocket (GHSA-p9ff-h696-f583) | Upgrade to ≥8.0.5 |
| 🟡 Moderate | `esbuild` | Any website can send requests to dev server (GHSA-67mh-4wv8-2f99) | Upgrade to ≥0.25.0 |
| 🟡 Moderate | `picomatch` | Method injection in POSIX character classes | Upgrade to ≥4.0.4 |
| 🟡 Moderate | `vite` | Path traversal in optimized deps | Upgrade to ≥8.0.5 |
| 🟡 Moderate | `postcss` | XSS via unescaped `</style>` in CSS output | Patch postcss |
| 🟡 Moderate | `brace-expansion` | Large numeric range causes regex denial | Upgrade |

Since all paths go through `vitest → vite` or the `esbuild` dev build tool, these vulnerabilities are only exploitable in development environments. They do not affect the shipped `main.js` plugin. However, upgrading `vite ≥8.0.5` and `esbuild ≥0.25.0` is straightforward and should be done.

---

## Priority 5 — ESLint Warnings

ESLint passes with **3 warnings, 0 errors** (lint is green for CI purposes):

| File | Line | Warning |
|---|---|---|
| `src/views/GraphViewContainer.ts` | 25:29 | `evaluateExpr` imported but never used |
| `src/views/GraphViewContainer.ts` | 25:43 | `parseQueryExpr` imported but never used |
| `src/views/GraphViewContainer.ts` | 76:26 | `addToMapSet` imported but never used |

All three are dead imports that should be removed. They are in the god-object file and add to its byte count unnecessarily.

**ESLint environment issue:** `pnpm lint` emits a `MODULE_TYPELESS_PACKAGE_JSON` warning because `package.json` lacks `"type": "module"` while `eslint.config.js` uses ES module syntax. Adding `"type": "module"` to `package.json` would silence this warning (verify compatibility with esbuild config first).

---

## Priority 6 — i18n Hardcoded Strings

`src/i18n-migration-notes.ts` catalogues **39 hardcoded UI strings** not yet routed through `t()`:

- **Group A (8):** `placeholder` attributes (settings path, ontology forward/reverse/bidirectional, group-by query, gravity filter)
- **Group B+ (31):** `aria-label` attributes, settings titles, dynamic prefixes, performance unit labels

A Japanese string leak was identified at `src/views/panel-widgets.ts:321` — `"(双方向)"` appears in what is presumably English-mode code. This should be gated behind `t()`.

---

## Priority 7 — Emerging Large Files (Not Yet God Objects)

These files are growing and warrant monitoring before they require their own ratchet:

| File | Lines | Note |
|---|---|---|
| `src/layouts/cluster-force.ts` | 3,570 | Largest non-god-object file; no ratchet limit yet |
| `src/views/panel-widgets.ts` | 1,811 | Panel UI logic; decomposition task active |
| `src/views/panel-sections.ts` | 1,036 | Panel section orchestration |
| `src/views/panel-sections-filter.ts` | 1,032 | Filter logic |
| `src/views/panel-sections-layout.ts` | 914 | Layout section |
| `src/layouts/coordinate-engine.ts` | 1,512 | Layout engine |

`src/layouts/cluster-force.ts` at 3,570 lines is larger than `RenderPipeline.ts` and `PanelBuilder.ts` and has no ratchet limit. It should be given one before it reaches GraphViewContainer territory.

---

## Recommended Next Actions

### Immediate (unblock autonomous gate)

1. **Extract `EdgeRenderer._drawTrunkWires()`** (330 lines) into a dedicated `TrunkWireRenderer` or split into 3 helper functions — this alone would reclaim meaningful headroom in `EdgeRenderer.ts`.
2. **Extract from `RenderPipeline.ts`** the 178-line method block at line 1849 — any extraction that saves >1 line unblocks future changes.
3. **Remove dead imports** in `GraphViewContainer.ts:25,76` (`evaluateExpr`, `parseQueryExpr`, `addToMapSet`).

### Short-term (1–2 sprints)

4. **Upgrade `vite ≥8.0.5` and `esbuild ≥0.25.0`** to clear the 3 high-severity devDep CVEs.
5. **Add a ratchet limit for `cluster-force.ts`** (current: 3,570 lines) before it grows further.
6. **Migrate the 8 Group A i18n strings** (`placeholder` attributes) — lowest friction, immediate UX quality improvement.
7. **Write tests for `CableTrayRenderer.ts` and `InteractionManager.ts`** — largest untested class files after the god objects.

### Medium-term (ongoing)

8. **Decompose `GraphViewContainer.ts`** per task `200-godobj-extract-tech-debt` subtasks 1292/1293/1295/1296 — snapshot, export, and filter orchestration are the identified extraction seams.
9. **Raise branch coverage floor from 48% toward 55%** — each new test file for an untested renderer will move this metric.
10. **Fix `package.json` missing `"type": "module"`** — eliminates ESLint startup warning and clarifies module format for tooling.

---

*Report generated by Claude Code on 2026-05-24. Run `pnpm audit`, `pnpm lint`, and `wc -l src/views/...` to refresh.*
