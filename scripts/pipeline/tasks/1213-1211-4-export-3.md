---
priority: high
reported: 2026-04-24
status: pending
source: decomposed
parent: 1211-1205-cluster-force-ts-export
depends: none
summary: 配置オフセット4関数をexport + 基本境界テスト3件追加
---

## Description (subtask of 1211-1205-cluster-force-ts-export)

src/layouts/cluster-force.ts の以下4関数に `export` キーワードのみ付与 (シグネチャ変更禁止):
    - concentricOffsets (L2739)
    - radialOffsets (L2811)
    - gridOffsets (L2849)
    - triangleOffsets (L2900)
  必要に応じ `ArrangementParams` / `ArrangementResult` 型も export する。
  GOD OBJECT 肥大化禁止 — 行数増加は export キーワード分のみ。

  tests/layouts/cluster-force.test.ts に以下の境界値テスト3件追加 (describe "arrangement offsets - boundary" で括る):
    - 空キー配列: concentricOffsets/radialOffsets/gridOffsets/triangleOffsets すべてに空配列 keys を渡すと空 Map (size=0) が返る (1テスト内で4関数まとめて検証可)
    - 単一キー: 各関数に単一キー [k] + 重心 cx=0, cy=0 を渡すと Map.get(k) が (0,0) 付近 (toBeCloseTo, 絶対値 < 小さい定数) に配置される
    - gridOffsets 4ノード配置: keys=['a','b','c','d'] で 2x2 グリッドとなり、dx/dy の絶対値分布が対称 (x座標が2種・y座標が2種の格子点) となる

  完了条件:
    - `pnpm test` 全件 PASS
    - `pnpm lint` 通過
    - 3 件以上の新規テストが追加されている (親タスク subtask-1 と合わせて 9 件以上)

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
