## Description (subtask of 1431-type-assertions)

ExportManager.ts (7箇所) と関連 export 機能を含むファイルで使われている
  `as PNG` (12) / `as JSON` (7) / `as CSV` (3) / `as Markdown` (4) / `as Mermaid` (3) /
  `as SVG` / `as PDF` 等の ExportFormat リテラル cast を、

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
