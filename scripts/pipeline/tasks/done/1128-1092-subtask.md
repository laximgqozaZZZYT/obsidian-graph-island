---
priority: medium
reported: 2026-04-19
status: cancelled
source: decomposed
parent: 1092-1072-status-done
depends: none
summary: subtask
---

## Description (subtask of 1092-1072-status-done)

タスク分解を行います。元issueは既に十分小さい単位ですが、検証ステップを独立タスクとして明示することで、各セッションが`max-turns 30`内に確実に収まるよう2段に分解します。

`★ Insight ─────────────────────────────────────`
- `replace_all=false` 指定は frontmatter 重複を防ぐ防御策。同じ文字列が本文中にも出る可能性があるため、最初の1件のみ置換する明示的契約。
- 編集と検証を別タスクにすると、検証失敗時のリトライが編集をやり直さずに済む（git diff で前回編集を再評価可能）。
- このタスク群は src/tests に触れないため `pnpm test` / `pnpm lint` 不要。Quality Gate を skip できる稀なケース。
`─────────────────────────────────────────────────`

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
