---
priority: high
reported: 2026-04-18
status: in-progress
source: decomposed
parent: 771-760-
depends: none
summary: 構造化出力スキーマと整形関数を実装
---

## Description (subtask of 771-760-)

subtask-2 の分類結果を受け取り、指定スキーマにマップする純粋関数を追加。
  - 型定義: { status: "ok" | "warning"; target_file: string; target_mark: "M" | "missing"; unexpected_changes: string[]; warnings: string[] }
  - 関数 `formatGitStatusShortResult(input)` を export
  - status 決定ロジック: target_mark==="missing" または unexpected_changes.length>0 → "warning"、それ以外 "ok"
  - warnings 配列は input.warnings をそのまま引き継ぐ (null/undefined は [] にフォールバック)
  - 新規ファイルは 80 行以内に収める (God Object 肥大化禁止)
  - 既存 pipeline スクリプトとの結合は行わない (pure function のみ)

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
