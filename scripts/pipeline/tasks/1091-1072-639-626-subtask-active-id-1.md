---
priority: medium
reported: 2026-04-19
status: pending
source: decomposed
parent: 1072-1056-639-626-subtask-active-1-status-done
depends: none
summary: 639-626-subtask active 候補を列挙し最小 ID の1件を選定
---

## Description (subtask of 1072-1056-639-626-subtask-active-1-status-done)

目的: 置換対象ファイルを一意に決定する。
  手順:
  1. Bash `ls scripts/pipeline/tasks/ | grep -E '639-626.*subtask'` で候補列挙 (done/ 除外)。
  2. 各候補を Read で frontmatter 確認し、`status:` が `decomposed` または `in-progress` かつ `depends: none` のもののうち、ファイル名先頭の ID 番号が最小の1件を選定。
  3. 候補一覧と選定根拠 (ID, status, depends) をテキスト出力。
  4. 選定ファイルの `status:` 行が frontmatter 内でちょうど1行のみであることを Read で再確認。
  受け入れ:
  - 選定ファイルパスが1つ確定
  - src/**, tests/**, package.json, vitest.config.ts, esbuild.config.mjs, God Object 4ファイルには一切触れない

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
