## Description (subtask of 1667-dead-exports)

ts-prune または同等の手法で src/ 配下の未使用 export を全件列挙し、
  ファイルパス別 + カテゴリ別 (utility / constants / types / views / etc.) に
  分類した作業リストを docs/dead-exports-inventory.md に記録する。
  - scripts/find-dead-exports.mjs に検出スクリプトを置く (既存があれば再利用)
  - 出力は「ファイルパス, シンボル名, 行番号, カテゴリ」のテーブル形式
  - 各シンボルについて「削除可能 / export 解除のみ可 / テスト等で間接利用」を一次判定
  - 後続

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
