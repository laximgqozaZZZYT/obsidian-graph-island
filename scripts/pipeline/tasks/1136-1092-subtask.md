---
priority: medium
reported: 2026-04-19
status: in-progress
source: decomposed
parent: 1092-1072-subtask
depends: none
summary: subtask
---

## Description (subtask of 1092-1072-subtask)

の引き継ぎ情報 (現在値 `decomposed` または `in-progress`) に基づき、Edit ツールで以下を 1 件だけ実施:
     - 現在値が `decomposed` の場合: `status: decomposed` → `status: done`
     - 現在値が `in-progress` の場合: `status: in-progress` → `status: done`
  2. `replace_all=false` を厳守 (1 件のみ置換)。
  3. `git mv` / ファイル移動は一切行わない (done/ への移動は別 issue の責務)。
  4. frontmatter の他フィールド (priority, reported, source, parent, depends, summary) と Description 本文は変更しない。
  5. Edit 後に Read で再読込し、差分が `status:` 行 1 行のみであることを確認。差分が 2 行以上ある場合は `git checkout -- <path>` で巻き戻して abort。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
