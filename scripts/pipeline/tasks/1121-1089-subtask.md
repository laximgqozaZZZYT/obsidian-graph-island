---
priority: medium
reported: 2026-04-19
status: pending
source: decomposed
parent: 1089-1068-639-626-subtask-status-in-progress-done
depends: none
summary: subtask
---

## Description (subtask of 1089-1068-639-626-subtask-status-in-progress-done)

で特定したファイルに対して:
  1. Edit (replace_all=false) で `status: in-progress` → `status: done` に置換
  2. `git status --short` で当該1ファイルのみ変更されていることを確認
  3. `git diff -- <file>` で status 行1行のみの差分であり、他フィールド
     (priority/reported/source/parent/depends/summary) と Description 本文が
     完全一致で保持されていることを検証
  4. 検証通過後コミット: `chore: done 1026-1014-639-626-subtask-status-done`

  制約:
  - src/**, tests/**, package.json, vitest.config.ts, esbuild.config.mjs は触らない
  - God Object 4ファイル (GraphViewContainer.ts 他) は触らない
  - git mv / リネーム禁止
  - issues/ 配下の当該1ファイルのみ編集
  - 差分検証で異常があればコミットせず中断

★ Insight ─────────────────────────────────────
- `depends: subtask-1` にすることで、対象ファイル特定失敗時に編集サブタスクが起動しないパイプライン制御が可能になります
- `replace_all=false` を明記することで、本文中に同一文字列があった場合の誤置換を防ぎます（frontmatterは先頭に1回のみ出現する前提）
─────────────────────────────────────────────────

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
