# Technical Debt Report — 2026-04-12

Generated by automated analysis of `src/` on 2026-04-12.

---

## Priority-Ranked Findings

### P0 — Critical: God Object Growth (Action Needed Now)

The four largest view files collectively account for **21,600 lines** — 45% of all source code.
`GraphViewContainer.ts` at 9,934 lines is the most extreme case and is on a growth trajectory
that will make any future refactor significantly harder.

| File | Lines | Risk |
|------|------:|------|
| `src/views/GraphViewContainer.ts` | 9,934 | Critical — single-class orchestrator |
| `src/views/PanelBuilder.ts` | 4,377 | High |
| `src/views/EdgeRenderer.ts` | 3,853 | High |
| `src/views/RenderPipeline.ts` | 3,436 | High |
| `src/layouts/cluster-force.ts` | 3,179 | Medium |
| **Total (top 5)** | **24,779** | |

`GraphViewContainer.ts` contains functions that individually exceed 200 lines and reaches
brace-nesting depth 9 — far beyond the recommended maximum of 4.

---

### P1 — High: Mega-Functions with High Cognitive Complexity

Functions over 100 lines are very difficult to reason about, test, or review safely.

| Function | File | Start Line | Length |
|----------|------|-----------:|-------:|
| `_updateGroupByLabels()` | `GraphViewContainer.ts` | 4879 | **313 lines** |
| `drawTimelineBars()` | `GraphViewContainer.ts` | 5623 | **210 lines** |
| `_renderMatrixViewMode()` | `GraphViewContainer.ts` | 9711 | **201 lines** |
| `cullOverlappingLabels()` | `RenderPipeline.ts` | 2762 | 179 lines |
| `_computeStaticLayout()` | `GraphViewContainer.ts` | 8157 | 176 lines |
| `doRender()` | `GraphViewContainer.ts` | 7641 | 176 lines |
| `_wireCanvasManagers()` | `GraphViewContainer.ts` | 2055 | 168 lines |
| `buildSection()` | `PanelBuilder.ts` | 1257 | 157 lines |
| `applyHover()` | `GraphViewContainer.ts` | 3830 | 146 lines |
| `_buildPanelCallbacks()` | `GraphViewContainer.ts` | 6678 | 135 lines |

All top-10 mega-functions live in god-object files. The `_updateGroupByLabels` function at
313 lines is the single largest and an immediate candidate for decomposition.

---

### P1 — High: Deep Nesting

Files where brace depth reaches 8–9 (recommendation: ≤ 4). Deep nesting indicates missing
early-return patterns, missing helper extraction, or inlined state machines.

| File | Max Brace Depth | Peak Line |
|------|----------------:|----------:|
| `src/views/GraphViewContainer.ts` | **9** | 8250 |
| `src/views/webgl/WebGLGraphics.ts` | 8 | 378 |
| `src/views/RenderPipeline.ts` | 8 | 2903 |
| `src/layouts/tree.ts` | 8 | 176 |
| `src/layouts/cluster-force.ts` | 8 | 2990 |

---

### P2 — Medium: Untested Production Files

The test suite has 111 test files and 2,987+ tests, but the following source files have
no direct or clearly corresponding test file. Several are significant in size or complexity.

**High-risk untested files (complex logic, no coverage):**

| File | Lines | Notes |
|------|------:|-------|
| `src/views/LayoutController.ts` | 631 | Complex orchestration, no test |
| `src/views/NodeDetailView.ts` | 610 | UI rendering, no test |
| `src/views/NodeComparisonView.ts` | ~300 | Comparison view, no test |
| `src/views/Minimap.ts` | ~200 | Minimap rendering, no test |
| `src/views/webgl/WebGLApp.ts` | ~200 | WebGL entry, no test |
| `src/views/webgl/WebGLContainer.ts` | ~150 | WebGL container, no test |
| `src/views/webgl/buffer-pool.ts` | ~100 | Buffer management, no test |
| `src/views/canvas2d/CanvasApp.ts` | ~100 | Canvas2D entry, no test |

**Low-risk untested files (utility/types/entry):**

- `src/main.ts` — plugin entry, Obsidian lifecycle
- `src/types.ts` — type declarations only
- `src/utils/toast.ts` — notification wrapper
- `src/utils/view-mode-map.ts` — mapping utility
- `src/utils/view-mode-sections.ts` — mapping utility
- `src/views/canvas2d/index.ts` — re-export barrel
- `src/views/canvas2d/interfaces.ts` — interface declarations
- `src/views/webgl/index.ts` — re-export barrel

**Coverage thresholds are critically low:** vitest is configured to require only ~29%
statement/line coverage. At this threshold the suite can pass while the most complex files
have zero coverage.

---

### P2 — Medium: Widespread `any` Type Usage

152 uses of `: any` or `as any` in production source files (excluding tests).
Heavy `any` usage defeats TypeScript's safety guarantees and is a common source of
runtime errors that the compiler cannot catch.

Recommended: Enable `noImplicitAny: true` in `tsconfig.json` and address violations
incrementally.

---

### P3 — Low: Security Audit (Dev Dependencies Only)

`pnpm audit` reports **6 vulnerabilities** — all in `devDependencies`, not shipped to end users.

| Severity | Package | Vulnerability | Fix |
|----------|---------|---------------|-----|
| High | `vite` (via vitest) | `server.fs.deny` bypass (GHSA-v2wj-q39q-566r) | Upgrade to ≥ 8.0.5 |
| High | `vite` (via vitest) | Arbitrary file read via WebSocket (GHSA-p9ff-h696-f583) | Upgrade to ≥ 8.0.5 |
| High | `picomatch` (via vitest) | ReDoS via extglob quantifiers (GHSA-c2c7-rcm5-vvqj) | Upgrade to ≥ 4.0.4 |
| Moderate | `vite` (via vitest) | Path traversal in optimized deps `.map` (GHSA-67mh-4wv8-2f99... wait) | Upgrade to ≥ 8.0.5 |
| Moderate | `esbuild` | Dev server CORS bypass (GHSA-67mh-4wv8-2f99) | Upgrade to ≥ 0.25.0 |
| Moderate | `picomatch` | POSIX character class injection (GHSA-3v7f-55p6-f55p) | Upgrade to ≥ 4.0.4 |

**Impact**: All vulnerabilities are dev-only (vitest and esbuild). No production user is exposed.
Still worth patching to keep CI/CD tooling secure on developer machines.

---

### P3 — Low: ESLint Not Configured

`npx eslint src/` fails with "couldn't find an eslint.config file". ESLint v9 requires
`eslint.config.js` (flat config). The project has no ESLint configuration at all, meaning
static analysis rules for TypeScript are not enforced in CI.

Recommended: Add a minimal `eslint.config.js` with `@typescript-eslint/recommended` rules.

---

### P4 — Informational: No CLAUDE.md / No Documented Size Limits

There is no `CLAUDE.md` file defining project conventions, god object size limits, or
coding guidelines for AI-assisted development sessions. Without explicit limits documented,
god objects continue to grow unchecked between sessions.

---

## God Object Growth Trend

No historical data is available in this run (first report). The table below establishes
the **2026-04-12 baseline** for future tracking.

| File | 2026-04-12 Lines | Suggested Limit | Over Limit? |
|------|----------------:|----------------:|:-----------:|
| `GraphViewContainer.ts` | 9,934 | 1,000 | **+893%** |
| `PanelBuilder.ts` | 4,377 | 800 | **+447%** |
| `EdgeRenderer.ts` | 3,853 | 800 | **+382%** |
| `RenderPipeline.ts` | 3,436 | 800 | **+330%** |
| `cluster-force.ts` | 3,179 | 600 | **+430%** |

> Suggested limits are industry norms (single-responsibility class ≤ 500–1000 lines).
> The "over limit" column is relative to those norms, not a project-defined threshold
> (none exists yet).

**Recommended action**: Add a `CLAUDE.md` with explicit line-count budgets and a CI check
(e.g., a short shell step in GitHub Actions) that fails the build when any god object
exceeds its budget. This prevents gradual boundary creep.

---

## Untested Files List (Complete)

```
# Complex / risky — should be prioritized for test coverage
src/views/LayoutController.ts
src/views/NodeDetailView.ts
src/views/NodeComparisonView.ts
src/views/Minimap.ts
src/views/webgl/WebGLApp.ts
src/views/webgl/WebGLContainer.ts
src/views/webgl/buffer-pool.ts
src/views/canvas2d/CanvasApp.ts

# Utility / low-risk
src/main.ts
src/types.ts
src/utils/toast.ts
src/utils/view-mode-map.ts
src/utils/view-mode-sections.ts
src/views/canvas2d/index.ts
src/views/canvas2d/interfaces.ts
src/views/webgl/index.ts
src/layouts/timeline-types.ts
```

Note: Many source files ARE covered by tests using differing naming conventions
(e.g., `arc-layout.test.ts` → `arc.ts`, `gvc-pure-functions.test.ts` → `GraphViewContainer.ts`).
The files above have no test file with an obviously matching name and were not identifiable
as covered by any clearly related test.

---

## Security Audit Summary

```
pnpm audit output — 2026-04-12
Severity: 3 high | 3 moderate | 0 low | 0 critical
Total: 6 vulnerabilities

All vulnerabilities are in devDependencies only (vitest chain and esbuild).
No production dependency vulnerabilities detected.

Affected packages:
  vite      >=8.0.0 <=8.0.4  →  upgrade to >=8.0.5  (fixes 3 vulns)
  picomatch >=4.0.0 <4.0.4   →  upgrade to >=4.0.4  (fixes 2 vulns)
  esbuild   <=0.24.2         →  upgrade to >=0.25.0  (fixes 1 vuln)
```

---

## Recommended Next Actions

| Priority | Action | Effort | Impact |
|----------|--------|--------|--------|
| P0 | Begin decomposing `GraphViewContainer.ts` — extract `_updateGroupByLabels` (313 lines), `drawTimelineBars` (210 lines), `_renderMatrixViewMode` (201 lines) into dedicated renderer classes | Large | Critical debt reduction |
| P0 | Add `CLAUDE.md` with god object size budgets and a CI line-count check | Small | Prevents future growth |
| P1 | Refactor `_updateGroupByLabels()` at line 4879 — reduce to ≤ 50 lines using early returns and helper extraction | Medium | Removes highest-complexity function |
| P1 | Add guard-clause / early-return rewrites to functions with nesting depth > 4 | Medium | Reduces cognitive load |
| P2 | Write tests for `LayoutController.ts` (631 lines, zero coverage) | Medium | Covers high-risk orchestration |
| P2 | Write tests for `NodeDetailView.ts` and `NodeComparisonView.ts` | Small–Medium | Covers untested UI paths |
| P2 | Raise coverage thresholds from 29% toward 50% in `vitest.config.ts` | Small | Enforces coverage growth |
| P2 | Address 152 `any` usages incrementally — start with god object files | Large | Improves type safety |
| P3 | Run `pnpm update vitest esbuild` to patch dev dependency vulnerabilities | Small | Clears audit warnings |
| P3 | Add `eslint.config.js` with `@typescript-eslint/recommended` and wire to CI | Small | Enables static analysis |
| P4 | Add `CLAUDE.md` documenting conventions, god object limits, and AI session guidance | Small | Onboarding + guardrails |

---

*Report generated automatically. All line counts are exact; function lengths are heuristic
(brace-depth tracking) and may vary ±5 lines from manual count.*
