## Description (subtask of 1417-type-assertions)

対象3ファイル内の `as unknown as T` / `as any` / `as HTMLElement` / Pixi系の `as Sprite` 等を以下で置換:
  - Pixi DisplayObject の絞り込みは `obj instanceof Sprite` / `obj instanceof Container` 型ガードへ
  - `as any` で隠している任意プロパティアクセスは、専用の interface を semantic-zoom-renderer.ts 内 (またはローカル型) に定義
  - InteractionManager.ts の MouseEvent / PointerEvent キャストは `event instanceof PointerEvent` 等で絞る
  - RenderThresholds 定数アクセスで `as` を使っている箇所は型推論に任せる (`as const` 化で除去)
  semantic-zoom-renderer.ts の `as unknown` 系18箇所が主ターゲット。
  完了条件: 対象3ファイルの `as ` キャスト件数を半分以下に、`pnpm test` / `pnpm lint` / `pnpm build` 全通過。
  RenderPipeline.ts は God Object のため**新規ロジック追加禁止**、純粋な型修正のみ。報告は実測値のみ。

---

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
