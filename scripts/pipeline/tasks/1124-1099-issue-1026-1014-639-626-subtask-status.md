---
priority: medium
reported: 2026-04-19
status: pending
source: decomposed
parent: 1099-1079-issue-frontmatter-status-done
depends: none
summary: 親issueファイル `1026-1014-639-626-subtask-status-done.md` の frontmatter status を done に更新
---

## Description (subtask of 1099-1079-issue-frontmatter-status-done)

1. `Glob` パターン `**/1026-1014-639-626-subtask-status-done*.md` で対象ファイルを特定する
     (`.claude/issues/pending/`, `.claude/issues/in-progress/`, `issues/pending/` などを含む)
  2. `Read` で先頭20行を読み frontmatter の現在の `status:` 値を確認する
  3. `Edit` で frontmatter 内の `status: pending` または `status: in-progress` を `status: done` に置換する
     - `old_string` には周辺行 (例: `reported: YYYY-MM-DD\nstatus: in-progress\nsource:`) を含め frontmatter 内でユニークにする
     - `replace_all=false`
  4. pending/ や in-progress/ ディレクトリ配下にある場合、規約に従い `git mv` で `done/` 配下へ移動する
     (移動先ディレクトリが存在しない場合は作成しない — 既存の規約に従うのみ)
  5. `git add` + `git commit -m "chore: done 1026-1014-639-626-subtask-status-done"` でコミット

  受け入れ基準:
  - 対象ファイルの frontmatter `status:` が `done` になっている
  - 他のフィールド (priority, reported, source, parent, depends, summary) および本文は変更されていない
  - ビルド/テスト実行は不要 (ドキュメント変更のみ)
  - CLAUDE.md の God Object ポリシー・ハードコード禁止等に抵触しない (該当なし)

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
