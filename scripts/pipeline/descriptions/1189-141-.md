
## Description (subtask of 141-coverage-drop)

subtask 1〜4 のテスト追加後、`pnpm test:coverage` を実行し、
  statements ≥ 52.2%, functions ≥ 50.2% を満たすことを確認する。
  もし余裕があれば (例: +0.3% 以上) vitest.config.ts のしきい値を
  現在値に合わせて引き上げる (ラチェット)。満たさない場合は subtask 1〜4 の
  いずれかに戻り、不足分 (statements または functions) に特化したテストを
  追加する。しきい値の **引き下げは禁止** (CLAUDE.md: Forbidden Patterns)。
  最終的に `pnpm test:coverage` / `pnpm lint` / `pnpm format:check` が
  すべて PASS することを確認。

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
