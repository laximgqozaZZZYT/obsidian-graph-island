
## Description (subtask of 1211-1205-cluster-force-ts-export)

tests/layouts/cluster-force.test.ts に以下の幾何的性質テスト3件追加 (describe "arrangement offsets - geometry"):
    - concentricOffsets リング等分配置: keys が同一リング (同じ ring 層) に属する場合、隣接ノード間の ユークリッド距離がほぼ等しい (max-min < 許容 ε)。最低 4 キーを同一リングに配置して検証
    - radialOffsets 重心一致: keys.length を 3→6→12 と増やしても、返された座標群の算術平均 (sum(x)/n, sum(y)/n) が原点 (cx,cy) 近傍に留まる (toBeCloseTo で < 小さい定数)
    - triangleOffsets 正三角形頂点配置: keys=['a','b','c'] の場合、3頂点間のユークリッド距離が互いにほぼ等しい (3辺の max-min < 許容 ε)

  src/layouts/cluster-force.ts は変更しない (subtask-1 で既に export 済み)。
  テストは `toBeCloseTo` または手動 ε 比較を使用。

  完了条件:
    - `pnpm test` 全件 PASS
    - `pnpm lint` 通過
    - 3 件以上の新規テストが追加され、subtask-1 と合わせて本 issue で計 6 件以上
    - `pnpm test:coverage` で src/layouts/cluster-force.ts の stmt/fn カバレッジが 57.8%/61.2% から向上していることを確認

## Acceptance criteria
- [ ] 実装が完了し、テストが通ること
- [ ] CLAUDE.md のルールに違反しないこと
